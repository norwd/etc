#!/usr/bin/env zunit

@test 'Test profile.d/*.sh is executable' {
	load ~/.config/profile

	# shellcheck disable=SC2044
	for script in $(find "${XDG_CONFIG_HOME}/profile.d" -name '*.sh')
	do
		assert "${script}" is_executable
	done
}
