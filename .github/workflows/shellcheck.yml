---

name: "Run ShellCheck"
run-name: "Run ShellCheck"
permissions: {}

on:
  push:
  schedule:
    - cron: '33 9 * * 2'

defaults:
  run:
    shell: bash # force pipe-fail

jobs:

  shellcheck:
    name: "Lint ${{ matrix.shell }} with ShellCheck"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shell: [ sh, ksh, bash ]

    env:
      JQ_QUERY: |
        .comments[] |
        .level = (
          if .level == "style" or .level == "info"
          then "notice"
          else .level
          end
        ) |
        .title = "SC\(.code)" |
        "::\(.level) file=\(.file),line=\(.line),endLine=\(.endLine),title=\(.title)::\(.message) --> https://www.shellcheck.net/wiki/SC\(.code)"

    steps:

      - name: "Checkout"
        uses: actions/checkout@v6

      - name: "Clone"
        run: git clone --bare ${{ github.server_url }}/${{ github.repository }} ~/etc

      - name: "Configure"
        run: git --git-dir="${HOME}/etc/" config --local status.showUntrackedFiles no

      - name: "Checkout"
        run: git --git-dir="${HOME}/etc/" --work-tree="${HOME}" checkout --force "${GITHUB_REF}"

      - name: "Setup Ruby"
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ruby
          bundler-cache: true

      - id: gem
        name: "Get Gem User Dir"
        run: ruby -e 'puts "user_dir=" + Gem.user_dir' | tee -a "${GITHUB_OUTPUT}"

      - name: "Add Gem User Dir to PATH"
        run: echo "${GEM_USER_DIR}/bin" | tee -a "${GITHUB_PATH}"
        env:
          GEM_USER_DIR: ${{ steps.gem.outputs.user_dir }}

      - id: cache-github-linguist
        name: "Cache Linguist"
        uses: actions/cache@v5
        with:
          key: ${{ runner.os }}-gem-install-github-linguist
          path: ${{ steps.gem.outputs.user_dir }}

      - if: steps.cache-github-linguist.outputs.cache-hit != 'true'
        name: "Install Linguist"
        run: gem install github-linguist --user-install

      - id: linguist
        name: "List Files by Language"
        run: github-linguist --breakdown --strategies --json . | jq --raw-output "${JQ_QUERY}" | sed '/^$/d' | tee -a "${GITHUB_OUTPUT}"
        env:
          JQ_QUERY: |
            to_entries[] |
            .key |= (ascii_downcase | gsub("\\s"; "-"; "g")) |
            .value |= (.files) |
            "
            \(.key)-json=\(.value)
            \(.key)-files=\(.value | join(" "))
            "

      - name: "Install Shellcheck"
        run: curl -sSL "${TAR_URL}" | sudo tar xvJ -C /usr/bin --overwrite --strip-components=1 shellcheck-latest/shellcheck
        env:
          TAR_URL: https://github.com/koalaman/shellcheck/releases/download/latest/shellcheck-latest.linux.x86_64.tar.xz

      - name: "ShellCheck (style)"
        run: >
          shellcheck
          --shell=${{ matrix.shell }}
          --severity style
          ${{ steps.linguist.outputs.shell-files }}
          --format json1
          |
          jq --raw-output "${JQ_QUERY}"

      - name: "ShellCheck (info)"
        run: >
          shellcheck
          --shell=${{ matrix.shell }}
          --severity info
          ${{ steps.linguist.outputs.shell-files }}
          --format json1
          |
          jq --raw-output "${JQ_QUERY}"

      - name: "ShellCheck (warning)"
        run: >
          shellcheck
          --shell=${{ matrix.shell }}
          --severity warning
          ${{ steps.linguist.outputs.shell-files }}
          --format json1
          |
          jq --raw-output "${JQ_QUERY}"

      - name: "ShellCheck (error)"
        run: >
          shellcheck
          --shell=${{ matrix.shell }}
          --severity error
          ${{ steps.linguist.outputs.shell-files }}
          --format json1
          |
          jq --raw-output "${JQ_QUERY}"

  zshellcheck:
    name: "Lint zsh with ZShellCheck"
    runs-on: ubuntu-latest
    steps:

      - name: "Checkout"
        uses: actions/checkout@v6

      - name: "Clone"
        run: git clone --bare ${{ github.server_url }}/${{ github.repository }} ~/etc

      - name: "Configure"
        run: git --git-dir="${HOME}/etc/" config --local status.showUntrackedFiles no

      - name: "Checkout"
        run: git --git-dir="${HOME}/etc/" --work-tree="${HOME}" checkout --force "${GITHUB_REF}"

      - name: "Setup Ruby"
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ruby
          bundler-cache: true

      - id: gem
        name: "Get Gem User Dir"
        run: ruby -e 'puts "user_dir=" + Gem.user_dir' | tee -a "${GITHUB_OUTPUT}"

      - name: "Add Gem User Dir to PATH"
        run: echo "${GEM_USER_DIR}/bin" | tee -a "${GITHUB_PATH}"
        env:
          GEM_USER_DIR: ${{ steps.gem.outputs.user_dir }}

      - name: "Cache Linguist"
        id: cache-github-linguist
        uses: actions/cache@v5
        with:
          key: ${{ runner.os }}-gem-install-github-linguist
          path: ${{ steps.gem.outputs.user_dir }}

      - if: steps.cache-github-linguist.outputs.cache-hit != 'true'
        name: "Install Linguist"
        run: gem install github-linguist --user-install

      - id: linguist
        name: "List Files by Language"
        run: github-linguist --breakdown --strategies --json . | jq --raw-output "${JQ_QUERY}" | sed '/^$/d' | tee -a "${GITHUB_OUTPUT}"
        env:
          JQ_QUERY: |
            to_entries[] |
            .key |= (ascii_downcase | gsub("\\s"; "-"; "g")) |
            .value |= (.files) |
            "
            \(.key)-json=\(.value)
            \(.key)-files=\(.value | join(" "))
            "

      - id: cache-zshellcheck
        name: Cache
        uses: actions/cache@v5.0.5
        with:
          key: ${{ runner.os }}-zshellcheck
          path: ~/.local/bin/zshellcheck

      - if: steps.cache-zshellcheck.outputs.cache-hit != 'true'
        name: "Install Zshellcheck"
        working-directory: ${{ runner.temp }}
        run: git clone "${REPO_URL}" && cd zshellcheck && ./install.sh
        env:
          REPO_URL: https://github.com/afadesigns/zshellcheck.git

      - name: "ZShellCheck (style)"
        run: echo "$SHELL_FILES_JSON" | jq --raw-output '.[]' | xargs -IFILE bash -eo pipefail -c "${Z_SHELL_CHECK_SH}"
        env:
          SHELL_FILES_JSON: ${{ steps.linguist.outputs.shell-json }}
          Z_SHELL_CHECK_SH: >
            zshellcheck
            --severity style
            --format json
            FILE |
            jq --raw-output '
              .[] |
              "::notice file=FILE,line=\(.Line),col=\(.Column),title=\(.KataID)::\(.Message)"
            '

      - name: "ZShellCheck (info)"
        run: echo "$SHELL_FILES_JSON" | jq --raw-output '.[]' | xargs -IFILE bash -eo pipefail -c "${Z_SHELL_CHECK_SH}"
        env:
          SHELL_FILES_JSON: ${{ steps.linguist.outputs.shell-json }}
          Z_SHELL_CHECK_SH: >
            zshellcheck
            --severity info
            --format json
            FILE |
            jq --raw-output '
              .[] |
              "::notice file=FILE,line=\(.Line),col=\(.Column),title=\(.KataID)::\(.Message)"
            '

      - name: "ZShellCheck (warning)"
        run: echo "$SHELL_FILES_JSON" | jq --raw-output '.[]' | xargs -IFILE bash -eo pipefail -c "${Z_SHELL_CHECK_SH}"
        env:
          SHELL_FILES_JSON: ${{ steps.linguist.outputs.shell-json }}
          Z_SHELL_CHECK_SH: >
            zshellcheck
            --severity warning
            --format json
            FILE |
            jq --raw-output '
              .[] |
              "::warning file=FILE,line=\(.Line),col=\(.Column),title=\(.KataID)::\(.Message)"
            '

      - name: "ZShellCheck (error)"
        run: echo "$SHELL_FILES_JSON" | jq --raw-output '.[]' | xargs -IFILE bash -eo pipefail -c "${Z_SHELL_CHECK_SH}"
        env:
          SHELL_FILES_JSON: ${{ steps.linguist.outputs.shell-json }}
          Z_SHELL_CHECK_SH: >
            zshellcheck
            --severity error
            --format json
            FILE |
            jq --raw-output '
              .[] |
              "::error file=FILE,line=\(.Line),col=\(.Column),title=\(.KataID)::\(.Message)"
            '
...
